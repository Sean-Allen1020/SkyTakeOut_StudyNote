## Vue框架下的前后端增删改查基本代码写法(后端指controller层)

### 前端传递的参数的形式：

- `Query`：**(`/emps?name=${name}&gender=${gender}`)**，它会直接拼接到url上传给后端
  - 后端直接用同名变量或拥有同名变量的类去接即可
  - 传递的数据为数组时，后端需要用 `@RequestParam List<..>`来接
- `Json`： **('/emps', emp)**，传递的为 Json对象，后端需要用 `@RequestBody`注解 来明确变量或类来接受该对象
- `Path`： **(`/emps/${id}`)** ，后端需要用 `@PathVariable注解` 来明确变量或类来接收该数据

> `${..}`为前端的变量字符串拼接占位符
> 要用该占位符时，字符串需要用 `` 反引号包裹
> 后端接收数据，**只与前端请求路径的变量名有关**，与前端变量本身的类型无关，*前端传递的参数本质都是字符串*
> 而传过来的字符串的转换规则，是由SpringBoot决定的
>   比如，前端传递了一个 `ids`的数据，那么后端只要有一个叫 `ids的集合类`形参来接收，
>   无论前端实际传的是`"1"`还是`"1, 2, 3"`，都不影响后端用集合接收该数据


-------------------

查：
    前端：
        axios.get(`/emps?name=${name}&gender=${gender}`)

    后端：
        @GetMapping("/emps")
        public Result page(Integer page, Integer pageSize, String name) { ... }
                            这里可以封装为DTO类，因为是query参数，所以不需要写多余的注释，直接写即可
			                如果前端传来的是 基本类型集合，则需要显示写上 @RequestParam

删：
    前端：
        axios.delete(`/emps/${id}`)
    
    后端：
        @DeleteMapping("/emps/{id}")
        public Result delete(@PathVariable Long id) { ... }
                              此处前端传来的id为path参数，
                              要用上 @PathVariable 注解

                              tip：同理，根据id查询的处理，也可以用path参数。具体如何用根据具体设计来

增/改：
    前端：
        axios.post('/emps', emp)  增
        axios.put('/emps', emp)   改
    
    后端：
        @PostMapping("/emps")
        public Result add(@RequestBody EmpDTO empDto) { ... }

        @PutMapping("/emps")
        public Result update(@RequestBody EmpDTO empDto) { ... }
                             
                             由于是json参数，所以要注明@RequestBody