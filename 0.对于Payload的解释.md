## 目前我所知的`Payload`有两种

### 第一种：JWT 的 payload

- 这是`token`内部的一部分，是令牌中存的数据。

`token`的组成：
    `header.payload.signature`
    `令牌类型 + 签名算法.自定义信息claims.前面两者融合后 + 密钥`

- 解析`token`时`getPayload()`拿到的，本质上就是这个东西。



### 第二种：浏览器 F12 里的 Payload

- 这是 HTTP 请求体，是前端发请求时携带的数据。

比如：
  - get请求：`/api/students?clazzId=&degree=&name=&page=1&pageSize=1`
            其后面的 query参数 就是 payload

  - post请求：`/api/students`
            其后面的 json参数 就是 payload

- **它跟 Jwt的payload 不是一回事。**