1. 前端发送 `/login` 请求
2. 后端验证账号密码成功 → 生成 token → 通过 VO 返回给前端
3. 前端保存 VO中的 token（比如 localStorage/内存）
4. 前端之后每次发送请求时，都在请求头加上 token: xxx
5. 后端拦截器对每个请求：
   - 取请求头 token
   - 校验签名/过期时间/claims（比如 id）
   - 成功：放行到 Controller
   - 失败：直接返回 401/403 或你们的 Result 错误码