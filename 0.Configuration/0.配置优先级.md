SpringBoot 中配置的优先级 (低->高)
- application.yaml
- application.yml
- application.properties
- java系统属性(-Dxxx=xxx) -Dserver.port=9000
- 命令行参数(--xxx=xxx) --server.port=9000

**如果访问jar包时的配置环境和jar包自身的配置环境不一样时，可以通过优先级更高的java系统属性和命令行参数进行配置**
比如测试环境和部署环境的sql密码差异等