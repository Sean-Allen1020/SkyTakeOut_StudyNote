# 逻辑外键与冗余字段说明

本文用于说明在业务系统数据库设计中，**逻辑外键（Logical Foreign Key）** 与 **冗余字段（Redundant Field）** 的含义、设计目的及使用场景。

---

## 一、逻辑外键（Logical Foreign Key）

### 1. 概念说明

逻辑外键是指：  
**在数据库表中仅通过字段值来表达表与表之间的关联关系，但不在数据库层面设置 FOREIGN KEY 约束**。

例如：

- shopping_cart.user_id → user.id  
- shopping_cart.dish_id → dish.id  

这些字段在**逻辑上**指向其他表的主键，但数据库并不会强制校验其存在性。

---

### 2. 与物理外键的区别

| 对比项                    | 逻辑外键       | 物理外键         |
| ------------------------- | -------------- | ---------------- |
| 是否使用 FOREIGN KEY 约束 | 否             | 是               |
| 数据一致性约束            | 由业务代码保证 | 由数据库保证     |
| 插入/删除性能             | 较高           | 较低             |
| 表之间耦合度              | 低             | 高               |
| 是否推荐互联网项目使用    | 是             | 否（大多数情况） |

---

### 3. 为什么使用逻辑外键

1. **避免数据库层强耦合**  
   防止一个表的数据操作被其他表“卡住”。

2. **提高写入和删除性能**  
   高频业务场景下，外键约束会明显拖慢性能。

3. **方便分库分表和系统拆分**  
   物理外键在分布式架构中几乎无法使用。

---

### 4. 使用逻辑外键的代价

- 数据一致性**完全依赖业务代码**
- 必须在 Service 层明确处理：
  - 删除校验
  - 数据补偿
  - 异常兜底

-----------------------------------------------------------------

## 二、冗余字段（Redundant Field）

### 1. 概念说明

冗余字段是指：  
**在一张表中，主动保存本可通过其他表关联查询得到的数据副本**。

这些字段并非该表的“唯一来源”，而是为了业务或性能目的而额外存储。

---

### 2. 冗余字段的本质

冗余字段的核心不是“多余”，而是：

- **业务时点的数据快照**
- **历史状态的固化**
- **查询性能的优化**

---

### 3. 示例说明（以购物车为例）

#### 不使用冗余字段的情况

```text
shopping_cart
- dish_id
- number
```

**查询时需要：**
```sql
    SELECT dish.name, dish.price
    FROM shopping_cart
    JOIN dish ON shopping_cart.dish_id = dish.id;
```

**问题：**
- 每次查询都要 JOIN
- dish.price 等字段变化会影响已加入购物车的展示结果


**使用冗余字段的情况**
```text
shopping_cart
- dish_id
- name        （冗余）
- image       （冗余）
- amount      （冗余，单价）
- number
```

**特点：**
- 加入购物车时，直接保存当时的商品信息
- 后续商品价格或名称变化，不影响用户已加入的数据